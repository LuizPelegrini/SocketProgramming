#include "app-client.h"


//retorna string contendo protocolo e dados
char* compor_msg()
{
  char* msg = malloc(80*sizeof(char);
  msg[0] = '\0';
  
  //concatenar msg com msg de protocolo da função desejada e com os respectivos dados
  
  return msg;
}
